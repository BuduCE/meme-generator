import React from 'react'
import './styles.css'

const Box = (props) => {

    const styles = {
        backgroudColor: props.status ? "#ccc"
    }
  return (
    <div>

    </div>
  )
}

export default Box